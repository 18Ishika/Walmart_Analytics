[project]
name = "walmart-analytics"
version = "0.1.0"
description = "Walmart analytics data loader using Neon Postgres"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "psycopg2-binary>=2.9.9",
]
